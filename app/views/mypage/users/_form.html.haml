= simple_form_for(@user, url: {action: :update}) do |f|
  = f.error_notification

  = f.input :name
  = f.input :location
  = f.input :about_me, input_html: {rows: 6, class: 'textarea'}

  .form-actions
    = f.button :submit, class: 'btn btn-primary'
