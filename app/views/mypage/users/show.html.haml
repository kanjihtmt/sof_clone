.row
  .col-md-3
    -# styleは全てclassとCSSで定義したほうが、あとで見返すときに、どこを見ればいいか迷わずにすむ気がします
    = image_tag(@user.try(:gravatar_url, size: 164), width: 164, height: 164)
  .col-md-6
    %h3= @user.name
    - if @user.about_me.present?
      = simple_format(@user.about_me)
    - else
      %p (あなたの自己紹介は現在未記入です)
    = link_to '編集するにはここをクリック', edit_mypage_users_path

  .col-md-3
    %p= "質問: #{@user.questions.size}"
    %p= "回答: #{@user.answers.size}"
    %p= "場所: #{@user.location}"
    %p= "登録から#{time_ago_in_words @user.created_at}目"
