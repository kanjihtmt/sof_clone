%h1 Listing answers

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @answers.each do |answer|
      %tr
        %td= link_to 'Show', question_answer_path(answer.question, answer)
        %td= link_to 'Destroy', question_answer_path(answer.question, answer), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Answer', new_question_answer_path
